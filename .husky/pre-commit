pnpm lint-staged
pnpm typecheck
pnpm test
# NOTE: Build check temporarily disabled due to pre-existing react-pdf/Next.js conflict
# TODO: Fix Html component conflict during static page generation (see DRM-013)
# pnpm build
